apiVersion: v1
kind: Service
metadata:
  name: {{ include "honcho-dashboard.fullname" . }}
  labels:
    {{- include "honcho-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "honcho-dashboard.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
